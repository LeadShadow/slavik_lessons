x = int(input('x: '))
y = int(input('y: '))

if x == 0:
    print("x can't be equal to zero")
    x = int(input('x: '))
    if x == 0:
        print("x can't be equal to zero")
        x = int(input('x: '))
        if x == 0:
            print("x can't be equal to zero")
            x = int(input('x: '))
            if x == 0:
                print("x can't be equal to zero")
                x = int(input('x: '))
                if x == 0:
                    print("x can't be equal to zero")
                    x = int(input('x: '))
                    if x == 0:
                        print("x can't be equal to zero")
                        x = int(input('x: '))
                        if x == 0:
                            print("x can't be equal to zero")
                            x = int(input('x: '))
                            if x == 0:
                                print("x can't be equal to zero")
                                x = int(input('x: '))
                                if x == 0:
                                    print("x can't be equal to zero")
                                    x = int(input('x: '))
                                    if x == 0:
                                        print("x can't be equal to zero")
                                        x = int(input('x: '))
                                        if x == 0:
                                            print("x can't be equal to zero")
                                            x = int(input('x: '))
                                            if x == 0:
                                                print("x can't be equal to zero")
                                                x = int(input('x: '))
                                                if x == 0:
                                                    print("x can't be equal to zero")
                                                    x = int(input('x: '))
                                                    if x == 0:
                                                        print("x can't be equal to zero")
                                                        x = int(input('x: '))
                                                        if x == 0:
                                                            print("x can't be equal to zero")
                                                            x = int(input('x: '))


result = y / x



# print, int, input
# int('2') -> 2
